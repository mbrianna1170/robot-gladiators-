let playerName = window.prompt('What is your robots name?');
// Displays playerName back to user
console.log('Introducing ' + playerName + '!');

function fight () {
    window.alert('The fight has started!');
}

// fight();